# Test Kubernetes App Docker Retag

E2E test repo for `kubernetes-app-docker-retag` workflow in buildon-github-actions.

Tests statefulset workload with PVC template, separate image reference style, helm token style, and headless service auto-generation. Retags redis:7.4 from Docker Hub.
